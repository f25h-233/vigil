# VIGIL M2 验收材料 —— 日报合成

> 生成于 2026-09-15 ｜ 模型 `Qwen/Qwen3.5-35B-A3B`（关思考）｜ 日报提示词 `v1`
> 文书刷新于 2026-09-15（`d9133a0`）｜ 三篇验收日报出自 `f55129b` 的统一重跑
> 验收日期 2026-09-11 / 09-12 / 09-13，空窗日 2026-08-08

> ⚠️ **本文件是四篇日报在 `f55129b` 下的统一重跑产物。**
> 库里那四篇此前分别由**三个不同的代码状态**产出——`4620251` 首跑、`d2f6868` 重跑（9/11、9/13、8/8）、
> `f55129b` 之后 9/12 的单跑——却都标着 `prompt_ver = 'v1'`。
> 若只补跑缺失的那天，最终材料就是**拼起来的**，无法回答「这份材料对应哪版代码」。
> 所以本次在 `f55129b`（**源码一行未改**）下把四天**全部重跑**：
> 下面四篇与 `docs/digests/` 落盘文件、库中 `digests.body_md` 三方一致。

> ⚠️ **刷新说明（`d9133a0`）：只改文书，没有重跑任何一天日报。**
> - **9/11、9/12、9/13 三篇正文逐字节未变**：`git hash-object` 与 `HEAD` blob **4/4 相同**（见下文核对）。
> - **8/8 空窗日正文换成新文案**（`0b74c7d` 定稿，带筛除分布 `1,014 + 45 = 1,059`）。
>   该分支**不调用模型**（纯本地渲染，token 0/0），换文案**不需要重跑日报**。
> - 只读复核（真库的**文件级副本**，真库 `sha256` 跑前跑后未变）：4 篇日报 ↔ 4 个库行、
>   `digest_items` 21 行、孤儿 0、文件与 `digests.body_md` **逐字节相等 4/4**。
> - 本文件的改动集中在文书层：8/8 正文、机械核验的复核说明、空窗日的核对项与说明、
>   「已知缺陷」一节（逐节清单见 `.superpowers/sdd/2026-09-15-vigil-m2-daily-digest/task-10-report.md`）。
>   **「你的判定」一节一字未动。**

**关于提示词版本**：`PROMPT_VERSION` 保持 **`v1`**（controller 裁定）。
本轮改的是**取值**（`deadline` 有时给 `None`），**字段集与措辞都没变**；
且取值本来就每天不同，把取值算进版本范围等于「每天都是新版本」。
更贴切的类比是 `refine` 的**预筛**——预筛决定「哪些消息进模型」，改预筛并不升版。
**截止日核验是数据闸门，不是提示词契约。**

## 机械核验（自动）

```
日期           item   链表   行数   补行    未匹配  判定
2026-09-11      5    5   13    0      0  ✓
2026-09-12      6    6   21    0      0  ✓
2026-09-13     10   10   29    0      0  ✓
2026-08-08      0    0    8    0      0  ✓
---
篇数: 4 （应为 4）
孤儿行: 0 （应为 0）
总体: ✅ 通过
```

关于这张表（`d9133a0` 下只读重查，**没有重跑 `vigil digest`**）：

- **四行 × 5 列 + 篇数 + 孤儿行 = 22 个数，与 `f55129b` 那次捕获相比只有 1 个变了**：
  8/8 的「行数」**5 → 8**（它换成了带筛除分布的新正文；该列取 `body_md` 的物理行数）。
  其余 21 个逐字未变。
- 重查方式：把真库做**文件级副本**到临时目录，用 `mode=ro` 只读连接查
  `items`（窗口内计数）、`digests`（`digest_id` / `body_md`）、`digest_items`（按 `digest_id` 计数）
  与孤儿检查；**真库 `sha256` 跑前跑后未变**（`8e8dd821…`）。
- 「补行 / 未匹配」两列取自 `_smoke/*.log` 的既有捕获（四天均为 0）；
  空窗日 0 条 item、不调模型，机械补行在结构上不可能发生。

### 闸门说明（关键）

**质量闸门是「补行数」，不是覆盖率。**

覆盖率（`item` vs `链表`）**恒为 100%**：`digest()` 无条件把窗口内全部 item_id
交给 `_persist`，没有任何可达状态能让这个等式失败。规划期最终 review 实测过：
让模型返回**空 lines**，10 条 item 全变机械补行，覆盖率照样 10/10、「✅ 通过」。
拿它当质量证据就是假绿。

所以真正的判据是 **补行数**：`补行 > 0` 意味着模型漏写了条目、日报里混进了
读起来生硬的机械行 → 不通过。覆盖率的两个数列**保留但仅作结构性检查**
（它守的是 `item_ids` 的接线，不是日报质量）。

**本次结果：四天补行数全为 0。** 并且此判据的措辞在源码里是
`vigil/cli.py:333` 的 `if stats.mechanical:` —— 只有 >0 才打印，
所以「日志里没有这行」确实等价于「补行为 0」，不是消息缺失造成的假绿。
模型调用失败也不会退化成机械行：`digest.py` 捕获 `LLMError` 后写 `stats.errors`，
`cli.py` 据此返回退出码 1（本次四天退出码均为 0）。

### 独立交叉核验（不依赖日志正则）

`build_rows()` 的语义是「模型写的每一行 → 一个 bullet，未被认领的 item → 机械补一个 bullet」。
因此 **正文 bullet 数 == 日志「日报 N 行」** 这个等式，只有在补行数为 0 时才可能成立
（任何机械补行都会让 bullet 数超过 N）。逐篇数过：

| 日期 | 日志「日报 N 行」 | 正文 bullet 数 | 区块数 | 结论 |
|---|---|---|---|---|
| 2026-09-11 | 4 | 4 | 2 | 一致 ⇒ 零机械补行（5 条 item 中有合并） |
| 2026-09-12 | 6 | 6 | 4 | 一致 ⇒ 零机械补行 |
| 2026-09-13 | 8 | 8 | 6 | 一致 ⇒ 零机械补行（10 条 item 中有合并） |
| 2026-08-08 | 0 | 0 | 0 | 一致（无条目） |

（该表在 `d9133a0` 下重新数过：对四篇正文分别数 `^- ` 与 `^## ` 两个正则，
**8 个格子全部与上表相同**。）

### 用量与耗时

| 日期 | 群数 | 消息数 | item | 日报行数 | 补行 | 输入 token | 输出 token | 耗时 |
|---|---|---|---|---|---|---|---|---|
| 2026-09-11 | 9 | 151 | 5 | 4 | 0 | 944 | 316 | 3.2s |
| 2026-09-12 | 9 | 514 | 6 | 6 | 0 | 980 | 373 | 3.8s |
| 2026-09-13 | 10 | 536 | 10 | 8 | 0 | 1,361 | 532 | 3.8s* |
| 2026-08-08 | 1 | 1,059 | 0 | 0 | 0 | 0 | 0 | 0.4s |

\* 9/13 为幂等复跑那一次（落盘正文出自该次）；四天均 <60s，未触发重试。

四天全部退出码 0，无一次超过 60 秒，未触发重试。
空窗日**未调用模型**（输出 token = 0），符合 spec §4.6「不假装有事」。
空窗日的筛除分布三个数**闭合**：**`1,014 + 45 = 1,059`**（本地筛掉 + 送到模型 = 当天消息总数），
且这三个数取自 refine 的**真实管线**（`expand_context` + `make_batches`），不是拿字段猜的。
进度行与正文同源：`[digest] 2026-08-08：窗口内没有条目（本地筛掉 1,014 条，送到模型 45 条）`。
（⚠️ `_smoke/2026-08-08.log` 是改造**之前**的旧捕获、不含这句；该进度行的真实输出记在
`task-11-report.md` §R3.7，逐字核对过。）

### 「⏰ 别忘」区块的实际出现（本轮核验重点）

本轮修复的核心是：**截止日必须能在该条源消息里逐字找到，才进「别忘」**；
且**没通过核验的截止日根本不会发给模型**（`f55129b`），模型因此无从在正文里写出它。
窗口内共 21 条 item，其中 4 条带 deadline，逐条结果：

| item | 标题 | 库中 deadline | 源消息里的时间表述 | 字面核验 | 结果 |
|---|---|---|---|---|---|
| 177 | 2026年下半年四六级考试报名通知 | 09-16 | 「**9月16日**12：00开始正式报名缴费」 | ✅ 找到 | **保留** |
| 235 | 开学典礼集合及入场安排通知 | 09-15 | 「**明早**7：20各班在宿舍楼下集合完毕」 | ❌ 无 | **降级** |
| 249 | 西太湖新媒体面试安排 | 09-19 | 「**周六**下午4.00-8.00」 | ❌ 无 | **降级** |
| 250 | 西太湖新媒体补录面试通知 | 09-15 | 「**明天**下午5点到6点有补录的机会」 | ❌ 无 | **降级** |

落到日报上：

| 日期 | 预期 | 实际 | 一致？ |
|---|---|---|---|
| 2026-09-11 | 「⏰ 别忘」区块**整个消失**（两条都降级） | 无该区块（区块仅 2 个：通知公告 / 活动） | ✅ |
| 2026-09-12 | 「⏰ 别忘」**只剩 1 行**（四六级 09-16） | 1 行，正是四六级 | ✅ |
| 2026-09-13 | 不该有（该日 0 条 deadline） | 无该区块 | ✅ |
| 2026-08-08 | 不该有（无条目） | 无该区块 | ✅ |

**降级 ≠ 丢弃**：三个被降级的条目仍全部出现在各自类目区块里——

| item | 标题 | 出现在 | 区块 |
|---|---|---|---|
| 235 | 开学典礼集合及入场安排 | 2026-09-11 | `## 📋 通知公告` |
| 249 | 西太湖新媒体面试安排 | 2026-09-11 | `## 🎤 活动` |
| 250 | 西太湖新媒体补录 | 2026-09-12 | `## 🎤 活动` |

### 额外确认（两条硬性检查）

**① 9/12 正文不得出现「9 月 15 日」或「09-15」**

```
$ grep -nE "9[[:space:]　]*月[[:space:]　]*15[[:space:]　]*日|09[-/月]15|9[-/]15" docs/digests/2026-09-12.md
（无输出）
命中数: 0
```

这是 `f55129b` 那条漏路的验收点：修复前 9/12 的「西太湖新媒体补录」正文里写着
「…明天通知，**截止 9 月 15 日**。」，而 item 250 的源文只有「明天下午5点到6点」。
修复后该日期不再出网，正文里也就写不出来了（该条目本身仍照常出现在 `## 🎤 活动`）。

**② 四篇里不得出现任何未经核验的 `· 截止 MM-DD ·` 戳**

```
$ grep -nE "截止" docs/digests/*.md
docs/digests/2026-09-12.md:7:- **四六级报名通知** 15 日前反馈问题，16 日 12:00 开始报名缴费，注意各年级时间。 · 截止 09-16 · 2026级常州大学新生群
命中总数: 1
```

全四篇共 1 个程序戳，恰好是唯一有源文字面依据的那条（item 177）；
另 3 条 deadline 一条都没打成戳。9/11、9/13、8/8 三篇**零戳**。

### 幂等复跑（2026-09-13）

按要求复跑 9/13（共 2 次），每次退出码 0：

| 检查项 | 结果 |
|---|---|
| `digests` 表篇数 | 复跑前后均 **4 篇**（不是 5 篇） |
| `digest_items` 行数 | 复跑前后均 **21 行**（5+6+10+0，未翻倍） |
| `docs/digests/2026-09-13.md` | 始终 **29 行 / 1320 字节**，**被覆盖而不是追加** |
| 该篇 `created_at` | 随复跑刷新（末次 16:29:19）⇒ 确为**重写** |

⚠️ `save_digest()` 是「先删 `digest_items`、再删 `digests`、再插入」，
而 `digests.digest_id` 是 **`INTEGER PRIMARY KEY`（无 AUTOINCREMENT）**，
删掉的若是当前最大 rowid，新行会**复用**该编号。所以 **`digest_id` 是否变化
不能当作「是否被替换」的证据**，要看清点后的行数（4 篇 / 21 行）。

⚠️ **结构幂等成立，字节幂等不成立**：两次复跑与首跑逐行比对，差异只在措辞
（如「班级名单统计：重名需处理」↔「班级名单重名处理」），
**区块划分、条目归属、行数、补行数完全一致**——模型输出有采样随机性，
不能用 sha256 当作「没被改过」的证据。

## 待你判定的三篇日报

按 spec §5，M2 的出口标准是：**连续 3 天真实日报，你读完判定「有用」**。

请逐篇读下面的日报，然后回答一个问题：**如果这是你每天收到的，你会觉得有用吗？**

### 2026-09-11

```markdown
# 守夜人日报 · 2026-09-11

当天 9 个群 151 条消息，提炼出 5 条。

## 📋 通知公告

- **明早开学典礼集合安排** 明早7:20在宿舍楼下集合，8点前按座位坐定，典礼结束后原地就坐。 · 储运263班级群

## 🎤 活动

- **今晚田径场有音乐节和招新** 今晚科教城田径场举办迎新音乐节，同时十大学生组织在田径场西侧同步开展线下招新。 · 2026级石工本科生群
- **西太湖新媒体面试安排** 面试时间为周六下午4点到8点，地点在立德楼1阶，需准备2分钟自我介绍。 · 西太湖新媒体部门招新群
- **国庆太湖湾音乐节战队招募** 招募5人同校常大在读战队，费用150元/人/天，凭参赛证免费看音乐节。 · 2026级常州大学新生群
```

### 2026-09-12

```markdown
# 守夜人日报 · 2026-09-12

当天 9 个群 514 条消息，提炼出 6 条。

## ⏰ 别忘

- **四六级报名通知** 15 日前反馈问题，16 日 12:00 开始报名缴费，注意各年级时间。 · 截止 09-16 · 2026级常州大学新生群

## 📋 通知公告

- **辅导员全员通知** 要求全员参加且非必要不请假，具体信息需查看后续非文本内容。 · 2026级石工本科生群

## 🎤 活动

- **编辑部今晚面试** 面试时间为晚上 6-8 点。 · 求索校组织26级招新群西太湖校区
- **外联 + 理论部补面** 面向未面试且想加入求索的同学，需投票。 · 求索校组织26级招新群西太湖校区
- **西太湖新媒体补录** 针对今天未面试同学，地点待定，明天通知。 · 西太湖新媒体部门招新群

## 🏠 生活

- **菜鸟驿站线上超市** 新开通线上超市，群号：<号码>。 · 2026级常州大学新生群
```

### 2026-09-13

```markdown
# 守夜人日报 · 2026-09-13

当天 10 个群 536 条消息，提炼出 10 条。

## 📋 通知公告

- **班级名单重名处理** 群内存在两个李瑞涵，要求退出一个。 · 2026级储运工程制图

## 📚 学业

- **课程安排：第五周开课** 今日（9月14日）为调休补课时，该课程安排在今天。 · 2026级常州大学新生群
- **周五周六无晚自习** 根据群内讨论，平时周五和周六不安排晚自习。 · 求索技术部的小窝

## 🎤 活动

- **西太湖新媒体招新** 面试地点变更至立德楼105，面试已结束，结果预计今明两天发出，录取名单已公布，请留意短信。 · 西太湖新媒体部门招新群

## 🛒 二手

- **求购机械设计教材** 收机械设计第十版，十一版也行。 · 常大二手咸鱼⑤群
- **出售补考及期末题库笔记** 含专业课、公共课，带重点笔记，包过，电子版。联系方式：百度网盘搜索昵称「风之海310」。 · 2026级常州大学新生群

## 🔍 失物招领

- **寻物：校园卡和钥匙** 物品包含校园卡和钥匙，疑似编号640。 · 2026级常州大学新生群

## 💼 兼职招聘

- **招聘高中作业辅导** 找能辅导高中作业的同学，兼职或全职均可。 · 常大二手咸鱼⑤群
```

### 你的判定

（请在此填写：有用 / 没用，以及理由。执行者不代填。）

- 2026-09-11：有用
- 2026-09-12：有用，但有一点不对：菜鸟驿站线上超市这个是经典诈骗，但是类似这样的情况层出不穷，过滤的话又可能误伤有用信息。
- 2026-09-13：有用

## 空窗日

```markdown
# 守夜人日报 · 2026-08-08

当天 1 个群 1,059 条消息。

其中 1,014 条被本地规则筛掉（纯应答 / 过短 / 刷屏广告一类），
45 条送到模型后判为无价值。

没有值得一提的信息。
```

空窗日的正确表现逐条核对：

- [x] 含「没有值得一提的信息」
- [x] 不出现任何条目行（bullet 数 = 0）
- [x] 不含「## ⏰ 别忘」等任何区块标题（区块数 = 0）
- [x] 未调用模型（token 0/0）
- [x] **自证抽取状态**（Task 9 新增）：出筛除分布，三数闭合 `1,014 + 45 = 1,059`
- [x] 进度行同步自证：`窗口内没有条目（本地筛掉 1,014 条，送到模型 45 条）`

**为什么要多这一层**：反馈是「空窗日信息不够」——但真正的问题不是话少，
而是**「这天确实没事」与「这天压根没抽取过」在日报里长得一模一样**，后者时那句话是**假的**。
Task 9 让空窗日自证抽取状态：**窗口内确实有消息**时，只有 `refined == messages`
（每条消息都有 `refine_runs` 记账）才允许说「没有值得一提的信息」；
覆盖不全时改说「**本窗口尚未抽取**（M/N），先跑 `vigil refine`」。
「一条消息都没有」另分两支：窗口在数据库跨度**之外**说「先跑 `vigil export`」、
**之内**才说「这天是真的没人说话」——「没数据」不等于是「没事」。

## 已知缺陷（不必重复报告，已知）

1. **M1 的「会补出原文没有的实体」会原样传下来**——日报读的是 items，items 错它就错。
   样本：item 172「咨询：四六级报名时间」（`event_ts` 2026-09-08 23:28，
   detail 自己写着「无具体信息，仅为提问」，confidence 0.10）——**窗口外**，
   本次验收窗口未复现该样本；但同源缺陷见下方「观察 1」。
2. **模型偶尔会在正文里重复群名**（如「高数（一）储运群要求…」），
   而行尾本来就有程序标注的来源群。属已知的轻微冗余。
3. **`vigil refine` 仍有全角引号退化的隐患**（全库 25 条消息含 `“ ”`），
   M2 未修：改它等于改 M1 已验证的提示词输入，会牵出 prompt_ver 升版与
   出口核验重跑。**遗留到下一个里程碑。**
4. **M1 补出的无依据日期，仍能从正文里漏出来（这条路径不是 `deadline` 字段）**：
   9/13「课程安排：第五周开课」正文写「今日（**9月14日**）为调休补课时」，
   而源消息（9/13 12:03）只有「今天上午有一节课第五周开」。
   它**不带 `· 截止 ·` 戳、不在「别忘」里**（不是 deadline 路径），读者不会误当硬期限，
   但它确实是一个**无依据的日期**——M1 补出来的，M2 只是忠实转述（证据见「观察 1」）。
5. **两处统计口径同名不同义**：`vigil refine` 的日志说「**本地丢弃 83**」，
   日报说「**本地筛掉 1,014**」——两个词看起来指同一件事，**口径差 12 倍**
   （前者只算 `prefilter` 硬丢弃的 83 条；后者含「没命中保留规则、根本没出本机」的那部分）。
   拿两者互相对照的人会被误导。**M2 不动 `refine.py`，记为遗留项交后续统一。**
6. **`items.deadline_ts` 在数据层没有修**：全库 26 条带 `deadline` 的 item 里，
   **13 条通不过源文字面核验**（Task 6 实测 `KEEP 13 / DROP 13`）。
   M2 的修法在**出口闸门**（未过核验的 deadline 不发给模型、也不打程序戳），
   **不是**清洗数据。→ **M3 若直接展示或消费 `deadline_ts`，会把这些幻觉日期原样复活。**
   （注：13 条里多数是「明天 / 周六」这类相对表达的正常换算，**不能一律判为编造**；
   可真判错的是验收窗口那 3 条——它们全比事件真实发生日晚 2–7 天。）
7. **日报措辞不可复现（结构幂等 ≠ 字节幂等）**：同一份代码重跑，每次的句子都可能不同
   （见上文「幂等复跑」）。区块划分、条目归属、行数、补行数稳定——但**不能用 sha256
   当作「文件没被改过」的证据**。由此本项目立了一条铁律：**禁止在真库真窗口重跑已验收产物**
   （重跑会不可逆地刷新 `created_at`；证明「LLM 产物未变」只能用
   **隔离副本 + 冻结模型 A/B + prompt sha 对照**）。
8. **上游误抽的广告，M2 不做任何过滤**（用户自己点出的那条）：
   9/12「菜鸟驿站线上超市」是经典诈骗，但**在日报层过滤等于加第二个静默过滤器**
   （spec §2.3 要避免的），而 spec §4.3 本就写着「不产出入库的：广告」——它属**上游误抽**。
   将来要做，正当位置是 `refine` 的预筛或 M1 提示词，不是日报层。**M2 不动。**

## 执行者新增观察（机械闸门全绿，但值得你知道）

### 观察 1：正文里的日期仍可能无依据——但路径已不是 `deadline` 字段

`f55129b` 堵死了「未经核验的 deadline 出网 → 模型写进正文」这条路（验收点 ①）。
但**日期出现在正文里**还有另一条路：M1 抽出的 item 文本本身就带日期，
日报只是照抄。本窗口实测 1 处 —— 9/13「课程安排：第五周开课」：

```
- **课程安排：第五周开课** 今日（9月14日）为调休补课时，该课程安排在今天。 · 2026级常州大学新生群
```

item 180 的 detail 就是「今日（9月14日）为调休补课时，该课程安排在今天」，
而它的源消息（9/13 12:03）**只写「今天上午有一节课第五周开」**。
即「9月14日」是 **M1 补出来的**（已知缺陷 1 的具体表现），M2 只是忠实转述。
它不带 `· 截止 ·` 戳、不在「别忘」里，读者不会误当硬期限，但**它是无依据的日期**。

另需说明：9/12「编辑部今晚面试」正文有「晚上 6-8 点」，那是**时间点不是日期**
（源文作「今晚 6-8 点」），无害，列此仅说明扫描口径。

**建议（不在本任务范围内）**：后置一道正文级日期核验，或推动 M1 的实体抽取收敛。
**本轮不修，交由 controller 决策。**
（本条已升格为「已知缺陷 4」；上面这一段就是它的证据。）

### 观察 2：日报措辞不可复现

见上文「幂等复跑」。结构（区块、条目归属、行数、补行数）稳定，但每次重跑的句子都可能不同。
对「存档可追溯」没影响（每次覆盖），但**不能用 sha256 当作「没被改过」的证据**。
（已升格为「已知缺陷 7」。）
